{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Tessera Python Client","text":"<p>Clean Hyperliquid market data \u2014 straight into Polars &amp; DuckDB.</p> <p><code>tessera-client</code> is the official Python client for Tessera: order-flow-enriched OHLCV, funding-rate, and positioning datasets built from raw Hyperliquid trade data and delivered as Parquet over a REST API.</p> <p>Point it at a <code>(dataset, coin, month)</code> and get a Polars <code>LazyFrame</code> or a DuckDB relation back \u2014 read directly from object storage over range requests, with predicate and projection pushdown. No temp files, no <code>pandas</code> round-trips, no glue code.</p> <pre><code>import tessera\n\nclient = tessera.TesseraClient()              # reads $TESSERA_API_KEY\ndf = client.read(\"gold_ohlcv_1m\", \"BTC\", \"2026-05\")\nprint(df.select(\"time\", \"close\", \"cvd\").tail())\n</code></pre>"},{"location":"#install","title":"Install","text":"<pre><code>pip install \"tessera-client[polars]\"     # headline path\npip install \"tessera-client[duckdb]\"      # query with SQL\npip install \"tessera-client[all]\"         # polars + duckdb + pandas\n</code></pre> <p>Get a free API key (no card required) at tesseralytics.dev.</p>"},{"location":"#why-its-fast","title":"Why it's fast","text":"<ul> <li>Remote range reads \u2014 only the Parquet footer and the row-groups/columns your query touches   are fetched.</li> <li>Lazy by default \u2014 build a query graph across many months, then <code>.collect()</code> once.</li> <li>Parquet end-to-end \u2014 never detours through CSV or <code>pandas</code>.</li> </ul>"},{"location":"#next-steps","title":"Next steps","text":"<ul> <li>Quickstart</li> <li>Recipes</li> <li>Dataset reference</li> <li>API reference</li> </ul>"},{"location":"api/","title":"API reference","text":""},{"location":"api/#clients","title":"Clients","text":""},{"location":"api/#tessera.TesseraClient","title":"<code>tessera.TesseraClient</code>","text":"<p>A synchronous client for the Tessera API.</p> <p>Parameters:</p> Name Type Description Default <code>api_key</code> <code>str | None</code> <p>Your Tessera API key. Falls back to <code>$TESSERA_API_KEY</code>.</p> <code>None</code> <code>base_url</code> <code>str</code> <p>API base URL. Defaults to the production endpoint.</p> <code>DEFAULT_BASE_URL</code> <code>timeout</code> <code>float</code> <p>Per-request timeout in seconds.</p> <code>30.0</code> <code>max_retries</code> <code>int</code> <p>Retries for transient failures (429/5xx, network errors).</p> <code>3</code> <code>http_client</code> <code>Client | None</code> <p>Inject your own <code>httpx.Client</code> (advanced; overrides the timeout/base-url defaults \u2014 you are responsible for auth headers).</p> <code>None</code> Example <p>client = TesseraClient() df = client.read(\"gold_ohlcv_1m\", \"BTC\", \"2025-09\")</p>"},{"location":"api/#tessera.TesseraClient.close","title":"<code>close()</code>","text":"<p>Close the underlying HTTP connection pool.</p>"},{"location":"api/#tessera.TesseraClient.datasets","title":"<code>datasets()</code>","text":"<p>List every dataset visible to your plan.</p>"},{"location":"api/#tessera.TesseraClient.partitions","title":"<code>partitions(asset, *, coin=None, month=None)</code>","text":"<p>List the partitions of <code>asset</code>, optionally filtered by coin/month.</p>"},{"location":"api/#tessera.TesseraClient.download_url","title":"<code>download_url(asset, coin, month)</code>","text":"<p>Mint a short-lived presigned download URL for one partition.</p>"},{"location":"api/#tessera.TesseraClient.partition_refs","title":"<code>partition_refs(asset, coin, month)</code>","text":"<p>Expand <code>(asset, coins, months)</code> into concrete partition references.</p>"},{"location":"api/#tessera.TesseraClient.scan","title":"<code>scan(asset, coin, month, *, columns=None)</code>","text":"<p>Lazily scan one or more partitions into a Polars <code>LazyFrame</code>.</p> <p>URLs are minted now but the data is read on <code>.collect()</code>. Because presigned URLs expire (~15 min), collect promptly; for long-lived graphs re-run <code>scan</code> to refresh.</p>"},{"location":"api/#tessera.TesseraClient.read","title":"<code>read(asset, coin, month, *, columns=None)</code>","text":"<p>Eagerly read one or more partitions into a Polars <code>DataFrame</code>.</p>"},{"location":"api/#tessera.TesseraClient.to_duckdb","title":"<code>to_duckdb(asset, coin, month, *, connection=None, columns=None)</code>","text":"<p>Open one or more partitions as a DuckDB relation for SQL querying.</p>"},{"location":"api/#tessera.TesseraClient.to_pandas","title":"<code>to_pandas(asset, coin, month, *, columns=None)</code>","text":"<p>Convenience escape hatch: read into a pandas <code>DataFrame</code> (via Polars).</p>"},{"location":"api/#tessera.AsyncTesseraClient","title":"<code>tessera.AsyncTesseraClient</code>","text":"<p>An asyncio-native client for the Tessera API.</p> <p>Mirrors :class:<code>~tessera.TesseraClient</code> with <code>await</code>. Parquet reads (which are CPU/IO-bound and synchronous in the engines) run in a worker thread so they never block the event loop.</p> Example <p>async with AsyncTesseraClient() as client: ...     df = await client.read(\"gold_ohlcv_1m\", \"BTC\", \"2025-09\")</p>"},{"location":"api/#tessera.AsyncTesseraClient.aclose","title":"<code>aclose()</code>  <code>async</code>","text":"<p>Close the underlying HTTP connection pool.</p>"},{"location":"api/#tessera.AsyncTesseraClient.datasets","title":"<code>datasets()</code>  <code>async</code>","text":"<p>List every dataset visible to your plan.</p>"},{"location":"api/#tessera.AsyncTesseraClient.partitions","title":"<code>partitions(asset, *, coin=None, month=None)</code>  <code>async</code>","text":"<p>List the partitions of <code>asset</code>, optionally filtered by coin/month.</p>"},{"location":"api/#tessera.AsyncTesseraClient.download_url","title":"<code>download_url(asset, coin, month)</code>  <code>async</code>","text":"<p>Mint a short-lived presigned download URL for one partition.</p>"},{"location":"api/#tessera.AsyncTesseraClient.partition_refs","title":"<code>partition_refs(asset, coin, month)</code>","text":"<p>Expand <code>(asset, coins, months)</code> into concrete partition references.</p>"},{"location":"api/#tessera.AsyncTesseraClient.scan","title":"<code>scan(asset, coin, month, *, columns=None)</code>  <code>async</code>","text":"<p>Lazily scan one or more partitions into a Polars <code>LazyFrame</code>.</p>"},{"location":"api/#tessera.AsyncTesseraClient.read","title":"<code>read(asset, coin, month, *, columns=None)</code>  <code>async</code>","text":"<p>Eagerly read one or more partitions into a Polars <code>DataFrame</code>.</p>"},{"location":"api/#tessera.AsyncTesseraClient.to_duckdb","title":"<code>to_duckdb(asset, coin, month, *, connection=None, columns=None)</code>  <code>async</code>","text":"<p>Open one or more partitions as a DuckDB relation for SQL querying.</p>"},{"location":"api/#tessera.AsyncTesseraClient.to_pandas","title":"<code>to_pandas(asset, coin, month, *, columns=None)</code>  <code>async</code>","text":"<p>Convenience escape hatch: read into a pandas <code>DataFrame</code> (via Polars).</p>"},{"location":"api/#helpers","title":"Helpers","text":""},{"location":"api/#tessera.MonthSpan","title":"<code>tessera.MonthSpan</code>  <code>dataclass</code>","text":"<p>An inclusive range of months, e.g. <code>MonthSpan(\"2025-01\", \"2025-09\")</code>.</p> <p>Pass it anywhere a <code>month</code> argument is accepted to expand to every month in the range (inclusive of both endpoints).</p>"},{"location":"api/#tessera.MonthSpan.months","title":"<code>months()</code>","text":"<p>Return the months in the span as a list of <code>YYYY-MM</code> strings.</p>"},{"location":"api/#tessera.PartitionRef","title":"<code>tessera.PartitionRef</code>  <code>dataclass</code>","text":"<p>A fully-qualified reference to a single partition.</p> <p>A partition is one <code>(asset, coin, month)</code> Parquet object, e.g. <code>gold_ohlcv_1m</code> / <code>BTC</code> / <code>2025-09</code>.</p>"},{"location":"api/#tessera.PartitionRef.object_key","title":"<code>object_key</code>  <code>property</code>","text":"<p>The object-storage key layout: <code>{asset}/coin={COIN}/month={YYYY-MM}.parquet</code>.</p>"},{"location":"api/#response-models","title":"Response models","text":""},{"location":"api/#tessera.DatasetsResponse","title":"<code>tessera.DatasetsResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p>"},{"location":"api/#tessera.PartitionsResponse","title":"<code>tessera.PartitionsResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p>"},{"location":"api/#tessera.DownloadResponse","title":"<code>tessera.DownloadResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p>"},{"location":"api/#errors","title":"Errors","text":""},{"location":"api/#tessera.errors","title":"<code>tessera.errors</code>","text":"<p>Exception hierarchy for the Tessera client.</p> <p>All errors raised by this library subclass :class:<code>TesseraError</code>, so a single <code>except tessera.TesseraError</code> catches everything. API errors additionally carry the HTTP <code>status_code</code> and the machine-readable <code>code</code> returned by the server (e.g. <code>not_found</code>, <code>forbidden</code>).</p>"},{"location":"api/#tessera.errors.TesseraError","title":"<code>TesseraError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base class for every error raised by <code>tessera</code>.</p>"},{"location":"api/#tessera.errors.ConfigurationError","title":"<code>ConfigurationError</code>","text":"<p>               Bases: <code>TesseraError</code></p> <p>The client was misconfigured \u2014 e.g. no API key could be resolved.</p>"},{"location":"api/#tessera.errors.MissingDependencyError","title":"<code>MissingDependencyError</code>","text":"<p>               Bases: <code>TesseraError</code></p> <p>An optional dependency (polars, duckdb, pandas) is required but absent.</p>"},{"location":"api/#tessera.errors.NetworkError","title":"<code>NetworkError</code>","text":"<p>               Bases: <code>TesseraError</code></p> <p>A network-level failure (connection/timeout) after exhausting retries.</p>"},{"location":"api/#tessera.errors.PresignExpiredError","title":"<code>PresignExpiredError</code>","text":"<p>               Bases: <code>TesseraError</code></p> <p>A presigned download URL expired before the read completed.</p> <p>Presigned URLs are short-lived (~15 minutes). Call <code>scan</code>/<code>read</code> again to mint a fresh one; do not cache a :class:<code>polars.LazyFrame</code> across that window.</p>"},{"location":"api/#tessera.errors.TesseraAPIError","title":"<code>TesseraAPIError</code>","text":"<p>               Bases: <code>TesseraError</code></p> <p>The API returned an error response.</p> <p>Attributes:</p> Name Type Description <code>status_code</code> <p>HTTP status code of the response.</p> <code>code</code> <p>Machine-readable error code from the <code>{\"error\": ...}</code> body, or <code>None</code> if the body was missing/unparseable.</p>"},{"location":"api/#tessera.errors.BadRequestError","title":"<code>BadRequestError</code>","text":"<p>               Bases: <code>TesseraAPIError</code></p> <p>400 \u2014 the request was malformed (bad coin, month format, etc.).</p>"},{"location":"api/#tessera.errors.AuthenticationError","title":"<code>AuthenticationError</code>","text":"<p>               Bases: <code>TesseraAPIError</code></p> <p>401 \u2014 the API key is missing, invalid, or revoked.</p>"},{"location":"api/#tessera.errors.ForbiddenError","title":"<code>ForbiddenError</code>","text":"<p>               Bases: <code>TesseraAPIError</code></p> <p>403 \u2014 your plan does not grant access to this dataset or coin.</p> <p>Upgrade at https://tesseralytics.dev to unlock Pro datasets and all coins.</p>"},{"location":"api/#tessera.errors.NotFoundError","title":"<code>NotFoundError</code>","text":"<p>               Bases: <code>TesseraAPIError</code></p> <p>404 \u2014 the dataset, coin, or partition does not exist.</p>"},{"location":"api/#tessera.errors.ServiceUnavailableError","title":"<code>ServiceUnavailableError</code>","text":"<p>               Bases: <code>TesseraAPIError</code></p> <p>503 \u2014 the catalog is temporarily unavailable. Safe to retry.</p>"},{"location":"api/#tessera.errors.InternalServerError","title":"<code>InternalServerError</code>","text":"<p>               Bases: <code>TesseraAPIError</code></p> <p>500 \u2014 an unexpected server error.</p>"},{"location":"api/#tessera.errors.error_from_response","title":"<code>error_from_response(status_code, body)</code>","text":"<p>Build the appropriate :class:<code>TesseraAPIError</code> from a response.</p> <p>Prefers the <code>{\"error\": \"&lt;code&gt;\"}</code> body; falls back to the HTTP status.</p>"},{"location":"datasets/","title":"Dataset reference","text":"<p>Tessera serves gold datasets \u2014 deduplicated, quality-checked, backtest-ready Parquet built from raw Hyperliquid trade data. Each dataset is partitioned by <code>coin</code> and <code>month</code> (<code>YYYY-MM</code>), covers October 2025 onward, and every row carries a <code>time</code> (UTC <code>Datetime</code>) and <code>coin</code> column.</p> Dataset Granularity Tier Contents <code>gold_ohlcv_1m</code> 1 minute Free + Pro Order-flow-enriched OHLCV candles <code>gold_funding_1h</code> 1 hour Pro Funding rates <code>gold_positioning_1h</code> 1 hour Pro Open-interest / positioning analytics <p>Free tier covers BTC, ETH, SOL, HYPE. Pro unlocks every coin \u2014 250+ symbols including HIP-3 markets and <code>xyz:</code>-prefixed equity/commodity perps (e.g. <code>xyz:NVDA</code>, <code>xyz:GOLD</code>) \u2014 plus the funding and positioning datasets. See pricing.</p>"},{"location":"datasets/#gold_ohlcv_1m","title":"<code>gold_ohlcv_1m</code>","text":"<p>Minute candles enriched with order-flow microstructure.</p> Column Type Meaning <code>time</code> datetime Bar start (UTC) <code>coin</code> str Coin symbol <code>open</code> / <code>high</code> / <code>low</code> / <code>close</code> f64 OHLC price <code>volume</code> f64 Traded volume <code>buy_vol</code> / <code>sell_vol</code> f64 Aggressor buy / sell volume <code>aggressor_delta</code> f64 <code>buy_vol \u2212 sell_vol</code> for the bar <code>cvd</code> f64 Cumulative volume delta <code>trade_count</code> u32 Number of trades <code>unique_takers</code> u32 Distinct taker accounts <code>vwap</code> f64 Volume-weighted average price <code>total_fees</code> / <code>taker_fees</code> / <code>maker_fees</code> f64 Fees attributed to the bar <code>taker_open_long_vol</code> / <code>taker_open_short_vol</code> f64 Taker volume opening longs / shorts <code>taker_close_long_vol</code> / <code>taker_close_short_vol</code> f64 Taker volume closing longs / shorts <code>closed_pnl_long</code> / <code>closed_pnl_short</code> f64 Realised PnL from closed longs / shorts <code>funding_rate</code> f64 Funding rate in effect <code>bootstrap_source</code> str Provenance of the bar"},{"location":"datasets/#gold_funding_1h","title":"<code>gold_funding_1h</code>","text":"Column Type Meaning <code>time</code> datetime Hour (UTC) <code>coin</code> str Coin symbol <code>funding_rate</code> f64 Hourly funding rate <code>annualized</code> f64 Annualised funding rate <code>cum_funding</code> f64 Cumulative funding <code>bootstrap_source</code> str Provenance"},{"location":"datasets/#gold_positioning_1h","title":"<code>gold_positioning_1h</code>","text":"<p>Open-interest and crowd-positioning analytics (selected columns):</p> Column Type Meaning <code>time</code> datetime Hour (UTC) <code>coin</code> str Coin symbol <code>open_interest</code> / <code>d_open_interest</code> f64 Open interest and its hourly change <code>n_long_positions</code> / <code>n_short_positions</code> u32 Open long / short positions <code>account_ls_ratio</code> f64 Account long/short ratio <code>top10_long_share</code> / <code>top10_short_share</code> f64 Share of OI held by the top 10 accounts <code>concentration_skew</code> / <code>hhi_gross</code> f64 Concentration metrics <code>whale_net_position</code> / <code>whale_long_account_share</code> / <code>n_whale_accounts</code> f64 / f64 / u32 Whale positioning <code>n_new_longs</code> / <code>n_new_shorts</code> / <code>n_closed_longs</code> / <code>n_closed_shorts</code> u32 Position open/close counts <code>n_flipped_l2s</code> / <code>n_flipped_s2l</code> u32 Long\u2192short / short\u2192long flips <code>aggressor_delta_1h</code> / <code>net_taker_open_1h</code> f64 Hourly order-flow aggregates <p>Tip</p> <p>Schemas evolve. Inspect the live column set without downloading data: <code>client.scan(\"gold_ohlcv_1m\", \"BTC\", \"&lt;month&gt;\").collect_schema()</code>.</p>"},{"location":"datasets/#discovering-coverage","title":"Discovering coverage","text":"<pre><code>client.partitions(\"gold_ohlcv_1m\", coin=\"BTC\")   # which months exist for BTC?\nclient.datasets()                                  # coins + month range per dataset\n</code></pre>"},{"location":"quickstart/","title":"Quickstart","text":""},{"location":"quickstart/#1-install-authenticate","title":"1. Install &amp; authenticate","text":"<pre><code>pip install \"tessera-client[polars]\"\nexport TESSERA_API_KEY=\"sk_...\"   # from https://tesseralytics.dev\n</code></pre> <p>The client reads <code>TESSERA_API_KEY</code> automatically. You can also pass it explicitly:</p> <pre><code>client = tessera.TesseraClient(api_key=\"sk_...\")\n</code></pre>"},{"location":"quickstart/#2-browse-the-catalog","title":"2. Browse the catalog","text":"<pre><code>import tessera\n\nwith tessera.TesseraClient() as client:\n    for ds in client.datasets().datasets:\n        print(ds.name, ds.coins, ds.months.earliest, \"\u2192\", ds.months.latest)\n</code></pre>"},{"location":"quickstart/#3-load-data","title":"3. Load data","text":"<p>A single partition into a Polars <code>DataFrame</code>:</p> <pre><code>df = client.read(\"gold_ohlcv_1m\", \"BTC\", \"2026-05\")\n</code></pre> <p>Many partitions, lazily, with column projection and a pushed-down filter:</p> <pre><code>import polars as pl\n\nlf = client.scan(\n    \"gold_ohlcv_1m\",\n    coin=[\"BTC\", \"ETH\"],\n    month=tessera.MonthSpan(\"2026-01\", \"2026-05\"),\n    columns=[\"time\", \"close\", \"cvd\"],\n)\ndf = lf.filter(pl.col(\"cvd\") &gt; 0).collect()\n</code></pre> <p>Multi-partition reads add <code>coin</code> and <code>month</code> columns so every row stays attributable.</p>"},{"location":"quickstart/#4-or-query-with-sql","title":"4. Or query with SQL","text":"<pre><code>rel = client.to_duckdb(\"gold_ohlcv_1m\", \"BTC\", \"2026-05\")\nrel.filter(\"close &gt; open\").aggregate(\"count(*) AS green_minutes\").show()\n</code></pre>"},{"location":"quickstart/#async","title":"Async","text":"<p>Every method has an <code>async</code> twin on <code>AsyncTesseraClient</code>:</p> <pre><code>async with tessera.AsyncTesseraClient() as client:\n    df = await client.read(\"gold_ohlcv_1m\", [\"BTC\", \"ETH\"], \"2026-05\")\n</code></pre>"},{"location":"quickstart/#a-note-on-presigned-urls","title":"A note on presigned URLs","text":"<p>Downloads use short-lived presigned URLs (~15 minutes). <code>read()</code>/<code>to_duckdb()</code> mint a fresh URL and read immediately. For a <code>scan()</code> <code>LazyFrame</code>, collect promptly \u2014 don't stash it for hours and collect later. If a URL expires mid-read you'll get a <code>tessera.PresignExpiredError</code>; just call <code>scan</code>/<code>read</code> again.</p>"},{"location":"recipes/funding-duckdb/","title":"Query Hyperliquid funding rates with DuckDB SQL","text":"<p>The <code>gold_funding_1h</code> dataset (Pro) gives you hourly funding rates. Open it as a DuckDB relation and stay in SQL \u2014 DuckDB range-reads the Parquet and pushes predicates down to row groups.</p> <pre><code>import tessera\n\nwith tessera.TesseraClient() as client:\n    rel = client.to_duckdb(\n        \"gold_funding_1h\",\n        coin=[\"BTC\", \"ETH\"],\n        month=tessera.MonthSpan(\"2026-01\", \"2026-05\"),\n    )\n\n    # Average funding by coin and the most extreme hours.\n    rel.aggregate(\"coin, avg(funding_rate) AS mean_funding\", \"coin\").show()\n</code></pre>"},{"location":"recipes/funding-duckdb/#bring-your-own-connection","title":"Bring your own connection","text":"<p>Pass an existing connection to join Tessera data against your own tables:</p> <pre><code>import duckdb\n\ncon = duckdb.connect(\"research.db\")\nrel = client.to_duckdb(\"gold_funding_1h\", \"BTC\", \"2026-05\", connection=con)\nrel.create_view(\"btc_funding\")\ncon.sql(\"SELECT * FROM btc_funding JOIN my_positions USING (time)\").show()\n</code></pre> <p>Tip</p> <p>Funding and positioning datasets are Pro-tier. A free key will raise <code>tessera.ForbiddenError</code> \u2014 upgrade here.</p>"},{"location":"recipes/ohlcv-polars/","title":"Load Hyperliquid OHLCV into Polars","text":"<p>Pull order-flow-enriched minute candles for any coin and resample them in Polars \u2014 read straight from object storage, no temp files.</p> <pre><code>import polars as pl\nimport tessera\n\nwith tessera.TesseraClient() as client:\n    df = client.read(\"gold_ohlcv_1m\", \"BTC\", \"2026-05\")\n\nhourly = (\n    df.sort(\"time\")\n    .group_by_dynamic(\"time\", every=\"1h\")\n    .agg(\n        pl.col(\"open\").first(),\n        pl.col(\"high\").max(),\n        pl.col(\"low\").min(),\n        pl.col(\"close\").last(),\n        pl.col(\"volume\").sum(),\n    )\n    .sort(\"time\")\n)\nprint(hourly.tail())\n</code></pre>"},{"location":"recipes/ohlcv-polars/#multiple-months-lazily","title":"Multiple months, lazily","text":"<p>Use <code>scan</code> for a <code>LazyFrame</code> and let Polars push your projection/filter down into the remote reads:</p> <pre><code>lf = client.scan(\n    \"gold_ohlcv_1m\",\n    coin=\"BTC\",\n    month=tessera.MonthSpan(\"2025-10\", \"2026-05\"),\n    columns=[\"time\", \"close\"],\n)\nreturns = (\n    lf.sort(\"time\")\n    .with_columns(pl.col(\"close\").pct_change().alias(\"ret\"))\n    .collect()\n)\n</code></pre> <p>Reading several coins at once adds a <code>coin</code> column so you can <code>group_by(\"coin\")</code>.</p>"},{"location":"recipes/orderflow-metrics/","title":"Backtesting with CVD &amp; aggressor delta","text":"<p>Tessera's <code>gold_ohlcv_1m</code> candles ship with order-flow microstructure baked in \u2014 cumulative volume delta (<code>cvd</code>) and per-bar <code>aggressor_delta</code> \u2014 so you can prototype flow-driven signals without reconstructing the tape.</p> <pre><code>import polars as pl\nimport tessera\n\nwith tessera.TesseraClient() as client:\n    lf = client.scan(\n        \"gold_ohlcv_1m\",\n        coin=\"BTC\",\n        month=tessera.MonthSpan(\"2026-01\", \"2026-05\"),\n        columns=[\"time\", \"close\", \"cvd\", \"aggressor_delta\"],\n    )\n\nsignal = (\n    lf.sort(\"time\")\n    # Go long when aggressive buying dominates over a 15-bar window.\n    .with_columns(pl.col(\"aggressor_delta\").rolling_sum(15).alias(\"flow\"))\n    .with_columns((pl.col(\"flow\") &gt; 0).cast(pl.Int8).alias(\"position\"))\n    .with_columns(pl.col(\"close\").pct_change().alias(\"ret\"))\n    .with_columns((pl.col(\"position\").shift(1) * pl.col(\"ret\")).alias(\"strategy_ret\"))\n    .collect()\n)\n\nequity = signal.select(pl.col(\"strategy_ret\").fill_null(0).cum_sum().alias(\"equity\"))\nprint(equity.tail())\n</code></pre> <p>Because <code>scan</code> is lazy, only the four columns above are read from each month's Parquet \u2014 the rest of the file never leaves object storage.</p>"}]}